return {
    refreshInterval  = 1,
    monitorTextScale = 0.5,
    energyUnit       = "FE",

    colors = {
        bg    = colors.black,
        title = colors.cyan,
        label = colors.lightGray,
        value = colors.white,
        good  = colors.lime,
        warn  = colors.yellow,
        bad   = colors.red,
        barBg = colors.gray,
    },

    cubeLabels = {
    },

    detectorLabels = {
    },

    itemListMode   = "flow",
    itemListLimit  = 5,
    itemListWindow = 600,
}
